<Window x:Class="PlayGif.Views.MediaPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:gif="clr-namespace:XamlAnimatedGif;assembly=XamlAnimatedGif"
        Title="PlayGif - Pick Media"
        Width="1100" Height="700"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Image grid -->
        <ListBox x:Name="ImageList" Grid.Column="0" Grid.Row="0"
                 Background="#111" BorderBrush="#333"
                 SelectionChanged="ImageList_SelectionChanged"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="160" Margin="4">
                        <Image Source="{Binding ThumbUrl}" Height="120"
                               Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="{Binding Size}" Foreground="#aaa"
                                   FontSize="10" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Preview panel -->
        <Border Grid.Column="1" Grid.Row="0" Margin="10,0,0,0"
                Background="#0d0d1a" BorderBrush="#333" BorderThickness="1" CornerRadius="4">
            <Grid>
                <TextBlock x:Name="PreviewPlaceholder" Text="Select an image to preview"
                           Foreground="#555" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Image x:Name="PreviewImage" Stretch="Uniform" Visibility="Collapsed"/>
            </Grid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Select" Padding="20,8" Margin="5"
                    Click="SelectButton_Click" IsDefault="True"
                    Foreground="White" Background="#2a6" BorderBrush="#2a6"/>
            <Button Content="Cancel" Padding="20,8" Margin="5"
                    Click="CancelButton_Click" IsCancel="True"
                    Foreground="White" Background="#444" BorderBrush="#555"/>
        </StackPanel>
    </Grid>
</Window>
